services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: abacadaba
      POSTGRES_PASSWORD: abacadaba
      POSTGRES_DB: abacadaba
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
