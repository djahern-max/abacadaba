# abacadaba.com

## Overview

**abacadaba.com** is a micro-learning platform.

A user:

1. Watches a ~5-minute video.
2. Takes a 5-question multiple-choice quiz.
3. Gets a small burst of confetti for each correct answer.
4. Passes with a score of **4/5 or better**.
5. On passing, gets bigger confetti plus a downloadable certificate.

The name is a play on guessing your way through a multiple-choice test.

## Stack

### Backend
- Python 3.12
- FastAPI
- SQLAlchemy 2.0
- Alembic
- Postgres 16

### Frontend
- React 18
- Vite
- Plain JavaScript — no TypeScript
- CSS Modules

### Video Storage
- DigitalOcean Spaces
- S3-compatible via `boto3`
- Presigned URLs

### Local Development
- Postgres runs in Docker

## Project Layout

```text
backend/            FastAPI app (see backend/CLAUDE.md)
frontend/           Vite React app (see frontend/CLAUDE.md)
current-feature.md  the ONE feature being built right now
CHANGELOG.md        completed features, append only
```

## Workflow — Read This First

`current-feature.md` is the single source of truth. Build exactly what it describes.

Do not build anything outside the feature's scope. If you hit something needed but out of scope, finish the feature and list it at the end of your response.

When every acceptance criterion passes:

1. Append a short entry to `CHANGELOG.md`.
2. Include the date and feature number.
3. Add one or two lines describing what shipped.
4. Say the feature is done.

Never rewrite `CHANGELOG.md` history. Append only.

## Conventions

- Use `snake_case` in Python and in the database.
- Use `camelCase` in JavaScript.
- Use `PascalCase` for components.
- API routes live under `/api/v1`.
- Every model change ships with its Alembic migration in the same change.
- Secrets go in `.env` and are never committed.
- Add new environment variables to `.env.example` as well.
- Prefer small, readable code over clever code.
- Justify any new dependency.

## Commands

### Start Docker Services

```bash
docker compose up -d
```

### Run Backend

```bash
cd backend
source .venv/bin/activate
uvicorn app.main:app --reload
```

### Run Frontend

```bash
cd frontend
npm run dev
```

### Run Backend Tests

```bash
cd backend
pytest
```

## Standards Compliance Mapping

Quoted from `docs/2026-Statement-on-Standards-for-CPE-Programs.pdf`
(August 2026 edition, effective 2026-08-01). Cross-referenced against
`docs/2024-to-2026-Standards-Crosswalk.pdf` where relevant.

**Scope note (added by feature 027).** This matrix maps abacadaba's behavior to
`docs/2026-Statement-on-Standards-for-CPE-Programs.pdf` and nothing else. A row
being fully green here is a claim that the cited Standards locator is
satisfied - not a claim that the application makes no false or misleading
statements more broadly. Feature 027 closed a real problem (a certificate
that asserted registered-sponsor status abacadaba did not have) that has no
row above, because no locator in the Standards document says "do not claim
to be a registered sponsor when you are not" - that obligation lives in the
National Registry's own terms and in ordinary honesty, not in this PDF. Read
a green column as "matches the Standards text," not as "trustworthy in every
respect."

### Feature 020 — Program metadata and learning objectives

| Locator | Requirement | Feature | Where in code | Gap |
| --- | --- | --- | --- | --- |
| 3.01 | "Sponsored learning activities must be based on relevant learning objectives and outcomes assigned to the learning activities that clearly articulate the professional competence that should be achieved by participants in the learning activities. When determining the learning objectives, program sponsors must consider the program knowledge level and prerequisite education/experience for the learning activity." | 020 | `app/models/learning_objective.py`; `validate_for_publish` in `app/services/admin_content.py` requires at least one non-blank objective before publish | |
| 3.01.1 | "Learning activities provided by CPE program sponsors must specify the knowledge level, content, and learning objectives so that potential participants can determine whether the learning outcomes are appropriate to their professional competence development needs, except as provided in Section 8 Paragraph 8.01 for mandatory internal courses. Knowledge levels consist of Basic, Intermediate, Advanced, Update, and Overview." | 020 | `Course.program_level` (`app/models/course.py`, CHECK constraint in migration `9dd63108687a`), `app/constants/program_levels.py`; disclosed via `GET /courses/{slug}` and `CourseDetail.jsx` | abacadaba has no mandatory-internal-course concept, so the 8.01 exception is not modeled |
| 3.02.1 | "To the extent it is possible to do so, CPE program sponsors should make every attempt to equate program content and knowledge level with the backgrounds of intended participants. All programs identified as Intermediate, Advanced or Update must clearly identify prerequisite education, experience, and advance preparation in precise language so that potential participants can readily ascertain whether they qualify for the program. For courses with a program knowledge level of Basic and Overview, prerequisite education or experience and advance preparation, should be noted if applicable, otherwise, state \"none\" in the course announcement or descriptive materials." | 020 | `validate_for_publish` in `app/services/admin_content.py` (rule 4/5); `Course.prerequisites`/`Course.advance_preparation`; `CourseDetail.jsx` renders literal "None" rather than omitting the row | Text is free-form, not validated for "precise language" — that's editorial judgment, not something a constraint can check |
| 8.01.1 | "For potential participants to effectively plan their CPE, the program sponsor must disclose the significant features of the program in advance (for example, through the use of brochures, websites, electronic notices, invitations, direct mail, or other announcements). When CPE programs are offered in conjunction with non-educational activities or when several CPE programs are offered concurrently, participants must receive an appropriate schedule of events indicating those components that are recommended for CPE credit. The CPE program sponsor's registration and attendance policies and procedures must be formalized, published, and made available to participants and include refund and cancellation policies as well as complaint resolution policies." | 020, 026 | `GET /courses/{slug}` (`app/routers/courses.py`, `app/schemas/course.py`); `CourseDetail.jsx` shows objectives, level, field of study, prerequisites, and advance preparation before enrollment. Feature 026 closes this row's original gap: `policies` table (`app/models/policy.py`, migration `c101e414d784`) holds `refund-and-cancellation`, `complaint-resolution`, `records-retention`, and `program-cancellation` as editable rows, seeded with an unmistakable placeholder rather than invented text; `GET /policies`/`GET /policies/{slug}` serve them unauthenticated, `PATCH /admin/policies/{slug}` (behind `require_admin`) edits them, and `validate_for_publish` (`app/services/admin_content.py`) refuses to publish any course while any of the four is still placeholder, naming which. `Policy.jsx` renders each at `/policies/{slug}`, linked from the site footer (`Footer.jsx`, fetched not hardcoded) and, for refund-and-cancellation and complaint-resolution specifically, from `CourseDetail.jsx`'s pre-enrollment disclosure block above the player | The "schedule of events" clause (CPE offered alongside non-educational activities, or several programs offered concurrently) is not applicable, confirmed by reading the code rather than assumed: abacadaba only ever offers one self study course at a time, with no concept anywhere (`app/services/courses.py`, `app/models/course.py`) of bundling several programs or non-CPE activities together, so there is no schedule of components to disclose. `program-cancellation` and `records-retention` are published as a matter of transparency alongside the two policies this locator names outright; records-retention is not itself an 8.01.1 participant-disclosure requirement (that is 9.02, the sponsor's own retention obligation), but publishing it lets a participant see the same retention window (9.02, feature 024's `RETENTION_YEARS`) the sponsor is bound by |
| 8.01.2 | "CPE program sponsors must distribute program materials in a timely manner and encourage participants to complete any advance preparation requirements. All programs must clearly identify prerequisite education, experience, and advance preparation requirements, if any, in the descriptive materials. Prerequisites, if any, must be written in precise language so that potential participants can readily ascertain whether they qualify for the program." | 020 | Same as 3.02.1: `Course.prerequisites`/`Course.advance_preparation`, disclosed via `GET /courses/{slug}` and rendered in `CourseDetail.jsx` | |

### Feature 021 — Development and review chain

| Locator | Requirement | Feature | Where in code | Gap |
| --- | --- | --- | --- | --- |
| 4.01 | "CPE program sponsors must employ activities, materials, and delivery systems that are current, accurate, and effectively designed. Course documentation must contain the most recent publication, revision, or review date. Courses in subjects that undergo frequent changes such as updates to codes, laws, rulings, decisions, interpretations, etc. must be reviewed and revised, as necessary, by a subject matter expert as soon as possible but at least once a year to verify the currency of the content. Other courses must be reviewed and revised, as necessary, at least every two years." | 021 | `Course.reviewed_at`, `Course.review_cycle` (CHECK `'annual'`/`'biennial'` in migration `561ad5e4ce3d`) in `app/models/course.py`; `validate_for_publish` refuses publish unless `reviewed_at >= content_updated_at`, so a published course's `reviewed_at` is always its true most-recent review/revision date; disclosed via `GET /courses/{slug}` and `CourseDetail.jsx`'s "Last reviewed" line | This feature stores the review date and the cycle an admin selects; it does not itself check whether a course has actually been re-reviewed within that cycle's window. That enforcement is feature 026's overdue-review dashboard, by design (see current-feature.md) |
| 4.01.1 | "Developed by subject matter expert(s). Learning activities must be developed by subject matter expert(s). The content developer must be competent and current in the subject matter, skilled in the use of the appropriate instructional strategies and technology. If technology is used in the development of the program, the content developer is responsible for reviewing the content for accuracy." | 021 | `SubjectMatterExpert` (`app/models/subject_matter_expert.py`) and `Course.developer_id`; `validate_for_publish` rule requires `developer_id` set; competence is disclosed via `credentials`/`license_jurisdiction`, editable at `/admin/smes` (`AdminSMEList.jsx`) and shown publicly on `CourseDetail.jsx`'s "Developed by" line | The system records that a named developer exists and their self-reported credentials; it does not verify a license number against any state board or otherwise confirm the claim - that verification is an editorial judgment when the SME record is created, not something the system checks |
| 4.02 | "CPE program sponsors must ensure that learning activities are reviewed by content reviewers other than those who developed the programs to ensure that the program is accurate and current and addresses the stated learning objectives. These reviews must occur before the first presentation of these materials and again after each significant revision of the CPE programs. The participation of at least one licensed CPA (in good standing and holding an active license or the equivalent of an "active" CPA license in a U.S. jurisdiction) is required in the development of every program in accounting and auditing. The participation of at least one licensed CPA, tax attorney, or IRS enrolled agent (in good standing and holding an active CPA license or the equivalent of an "active" license in a U.S. jurisdiction) is required in the development of each program in the field of study of taxes. In the case of the subject matter of international taxes, the participation of the equivalent of an "active" licensed CPA for the international jurisdiction involved is permitted. As long as this requirement is met at some point during the development process, a program would be in compliance. Whether to have this individual involved during the development or the review process is at the CPE program sponsor's discretion." | 021 | `Course.reviewer_id` distinct from `developer_id`, enforced by CHECK `ck_courses_developer_reviewer_differ` and `validate_for_publish`; "before first presentation and after each significant revision" is enforced by refusing publish whenever `reviewed_at < content_updated_at`, treating every content edit as a potential significant revision; the CPA/tax-attorney/enrolled-agent participation rules are `validate_for_publish`'s field-of-study-conditional rules against `SubjectMatterExpert.is_licensed_cpa`/`is_tax_attorney`/`is_enrolled_agent` and the `credential_tag` on each `app/constants/fields_of_study.py` entry | A published course that is then edited stays published and keeps serving participants while re-review is pending: `content_updated_at` is bumped and re-publishing is blocked, but the course is not unpublished, so it can serve edited, unreviewed content until someone completes the review. The admin editor surfaces this state (`ReviewPanel.jsx`'s stale-review notice) as mitigation; feature 026's `GET /admin/currency` dashboard (`app/services/currency.py::published_but_edited`, `AdminCurrency.jsx`'s "Published but edited since review" section) now reports every such course sponsor-wide in one place, sorted by how long it's been unreviewed, worst first, instead of only being visible one course at a time inside that course's own editor. This closes the reporting gap this row named, but reporting is all it does: the underlying problem is unchanged, and 026 was explicitly scoped to report the state rather than prevent it - see current-feature.md, "reporting a state is not the same as preventing it." The international-taxes equivalent-jurisdiction allowance and the "at the sponsor's discretion" timing nuance are not modeled - the system only checks that a qualifying credential is present somewhere in the developer/reviewer pair |
| 4.02.1 | "Qualifications of content reviewers. Individuals or teams qualified in the subject matter must review programs. The intent of the review is to serve as a quality control procedure to ensure the course content is accurate and current as well as appropriate for CPE. In rare circumstances, it may be impractical to review certain programs in advance. In those rare circumstances, greater reliance should be placed on the recognized professional competence of the instructor or presenter, and the basis for the lack of content review must be documented." | 021 | `Course.review_notes` (free text) gives a place to document the basis for a review, including the rare-impractical-review case; `SubjectMatterExpert.credentials`/`is_licensed_cpa`/`is_tax_attorney`/`is_enrolled_agent`/`license_jurisdiction` record the reviewer's stated qualification | "Qualified in the subject matter" is an editorial judgment the Standard leaves to the sponsor; nothing here validates subject-matter fit beyond the credential booleans. There is no distinct workflow or field for the rare-impractical-review case - a sponsor documents that basis in the same free-text `review_notes` as any other note, which is a reasonable but unenforced convention rather than a structured requirement |

### Video pipeline 01 — Narration generation with measured reveals

Not an app feature (see current-feature.md's numbering note) - this is build
tooling in `video/`, its own track outside the app's feature sequence.

| Locator | Requirement | Feature | Where in code | Gap |
| --- | --- | --- | --- | --- |
| 7.02.7 | "If audio and video segments of a self study program constitute additional learning for the participant (that is, not narration of the text), then the actual audio/video duration time may be added to the time calculation as provided in the prescribed word count formula. If the entire self study program constitutes a video, then the prescribed word count formula in Section 7 Paragraph 7.02.6 would consist of the actual video time plus the total number of review questions (including those above the minimum requirements), exercises, and qualified assessment questions multiplied by 1.85, divided by 50 (that is, there would be no word count for text used in the formula)." | Video pipeline 01 | `video/scripts/generate-audio.ts` measures each block's real narration length from ElevenLabs' character-level alignment data and writes it to `video/src/audio-meta.json`; `video/src/lesson-01.ts`'s `durationOf`/`totalSeconds` read that measured value ahead of `estimatedSeconds`, and `usingEstimates` (fixed this feature to stop counting the narration-less title sheet) flags any block still on an estimate; `video/src/Root.tsx` warns loudly and names the constraint whenever `usingEstimates` is true | This package only produces and flags the real "actual video time" input for the formula above - it does not compute CPE credit (the calculation in `generate-audio.ts` is explicitly labelled a sanity check, not the number of record) and nothing here stops a video from being rendered or published while `usingEstimates` is true, it only warns. The word-count-formula credit calculation itself, applied at the course level with the real question count across all of a course's lessons, is feature 022's responsibility and does not exist yet |

### Feature 022 — Credit measurement

| Locator | Requirement | Feature | Where in code | Gap |
| --- | --- | --- | --- | --- |
| 7.01 | "At their discretion, CPE program sponsors may round down (but not up) CPE credits awarded for a CPE program to the nearest one-fifth, one-half, or whole credit increment as appropriate for the type of formal learning program. ... In addition, CPE program sponsors must ensure that the total credit for a CPE program ... is only in the allowable increment and should round down the credit if necessary." (Self study programs: "A minimum of one-fifth or one-half credit may be awarded initially.") | 022 | `app/services/credit.py::round_down` (`floor(raw * 5) / 5`, never rounds up); `app/constants/credit.py::MIN_AWARDABLE = Decimal("0.2")`; `validate_for_publish` in `app/services/admin_content.py` refuses publish below that minimum | abacadaba always rounds to the finest legal increment (one-fifth) uniformly, per current-feature.md's explicit direction. It does not implement the one-half-increment path, and it applies one policy site-wide rather than per state board; the rounding function carries a comment noting superCPE will likely need a per-jurisdiction policy |
| 7.02.6 | "The word count for the text of the required reading of the program is divided by 180, the average reading speed of adults. The total number of review questions (including those above the minimum requirements), exercises, and qualified assessment questions are multiplied by 1.85 ... These two numbers plus actual audio/video duration time (not narration of the text), if any, are then added together and the result divided by 50 to calculate the CPE credit for the self study program. ... [(# of words/180) + actual audio/video duration time + (# of questions × 1.85)] /50 = CPE credit" | 022 | `app/constants/credit.py` (`WORDS_PER_MINUTE`, `MINUTES_PER_QUESTION`, `MINUTES_PER_CREDIT`); `app/services/credit.py::compute`/`store`; inputs on `app/models/lesson.py` (`word_count`, `av_is_additional_learning`, `duration_seconds`) and stored results on `app/models/course.py` (`credit_award`, `credit_raw_minutes`, `credit_word_count`, `credit_av_seconds`, `credit_question_count`, `credit_formula_version`, `credit_computed_at`); shown term-by-term in `CreditPanel.jsx`; gated at publish by three new rules in `validate_for_publish` | `compute()` sums every question and every lesson's word/A-V input across the whole course, not `courses_service.published_question_count`'s `Lesson.is_published`-gated count as current-feature.md's Task 4 literally directs — deliberate: `publish_course()` only flips lessons to published after `validate_for_publish` passes, so gating credit's own inputs on that same published flag would make a course's first-ever publish permanently unreachable and would compute 0 credit for the feature's own 486-second/8-question acceptance example. See the comment at the count's call site in `app/services/credit.py::compute`. Separately, the word count itself is typed by the author (`word_count` on `Lesson`) and is not checked against the actual segment's text length |
| 7.02.7 | "If audio and video segments of a self study program constitute additional learning for the participant (that is, not narration of the text), then the actual audio/video duration time may be added to the time calculation ... If the entire self study program constitutes a video, then the prescribed word count formula ... would consist of the actual video time plus the total number of ... questions multiplied by 1.85, divided by 50 (that is, there would be no word count for text used in the formula). [actual audio/video duration time + (# of questions × 1.85)] /50 = CPE credit" | 022 | `Lesson.av_is_additional_learning` (`app/models/lesson.py`); `compute()`'s branch adds `duration_seconds` to the A/V term only when true, else adds `word_count` to the word term; the flag is editable per segment via the checkbox in `LessonVideoFields.jsx`/`DetailsForm.jsx` ("This segment's audio teaches something the slides don't say"), and the all-video case needs no separate code path since a zero word count already zeroes that term of the same formula | Nothing in the application verifies that a lesson's stored `duration_seconds` came from a measured render rather than an estimated one. That guard lives in `video/` (Video pipeline 01's `usingEstimates`/`Root.tsx` warning), outside this app entirely, and an author who types a duration by hand — or a real-world sponsor whose renderer never measured it — can defeat it. The only defence noted in current-feature.md is that `duration_seconds` is auto-filled from the browser's own reading of the uploaded video file (feature 017's `VideoUploader.jsx` reading `video.duration`) rather than typed as a first step, and that auto-fill is still editable afterward |

### Feature 023 — Review questions, assessment questions, and thresholds

| Locator | Requirement | Feature | Where in code | Gap |
| --- | --- | --- | --- | --- |
| 5.01.2 | "Participant engagement for self study programs. Examples of content related participant engagement in self study programs include but are not limited to: 1) Instructional strategies, such as interactive content, hands-on activities, personalized learning paths, or audiovisual aids; 2) Content Reinforcement tools with feedback as required by 5.01.2.1, such as review questions or simulations. 3) Qualified Assessments as required by 6.01.2, such as multiple-choice questions or simulations." | 023 | `Question.kind` (`app/models/question.py`, CHECK `ck_questions_kind_valid` in migration `19665ec864ab`) splits every question into `'review'` (5.01.2.1/5.01.2.2) or `'assessment'` (6.01.2); `app/services/quiz.py` and `app/services/review.py` serve the two kinds through separate endpoints so neither substitutes for the other | Only review questions and multiple-choice qualified assessments are implemented; simulations, as an alternative content-reinforcement or assessment tool under this paragraph, are not modeled - current-feature.md rules them explicitly out of scope |
| 5.01.2.1 | "Review questions or other content reinforcement tools must be placed throughout the program in sufficient intervals to allow the participant the opportunity to evaluate the material that needs to be re-studied. At least three review questions or other content reinforcement tools with scored responses per CPE credit must be included. If the program is marketed for one-half CPE credits, then two review questions or other content reinforcement tools with scored responses must be included. ... 'True or false' questions do not count toward the number of required review questions per CPE credit. There is no minimum passing rate required for review questions or other content reinforcement tools. If the program is marketed for one-fifth CPE credit or 0.2 CPE credit, then a review question or other content reinforcement tool with scored response is not required. After the first one-fifth credit is earned, review questions ... are required based on the one-fifth credit measurement amount of the program as follows: [0.2→0, 0.4→1, 0.5→2, 0.6→2, 0.8→3, 1.0→3]. After the first full credit and the minimum of three review questions ..., additional review questions ... are required based on the chart above." | 023 | `app/constants/question_minimums.py` (`REVIEW_PER_CREDIT`, `REVIEW_MINIMUMS`, transcribed from the PDF, not from current-feature.md); `validate_for_publish` in `app/services/admin_content.py` enforces the floor against the course's computed credit, counting a review question only when it has more than two choices (two-choice/"true or false" items excluded per this paragraph); `app/services/review.py`/`app/routers/review.py` serve review questions at the end of the segment they belong to, once that segment's watch gate is met (`ReviewPanel.jsx` on `LessonSegment.jsx`/`CourseDetail.jsx`); `AttemptAnswer`/`attempts` are never touched by a review answer, matching "no minimum passing rate" | Segment-boundary placement satisfies "placed throughout the program in sufficient intervals" for a multi-segment course, but not for a **one-lesson course above the 0.2-credit tier**: a single lesson has exactly one seam, the end, and there is no "throughout" in a one-segment program. Feature 019a's collapsed single-lesson editor makes a one-video course the easy path, so this is a live collision, not a hypothetical one; no publish warning was added for it (current-feature.md phrased that as "consider," not "build"). Mid-video review cues are the real fix and are explicitly out of scope here. Separately, the above-one-credit decomposition (`whole * REVIEW_PER_CREDIT + REVIEW_MINIMUMS[remainder]`) is an interpretation of "based on the chart above," not a quoted rule - see the fuller note on 6.01.2's row, where the same interpretation is verified against the Standard's own worked example. Content reinforcement tools other than review questions (e.g., simulations) are not implemented |
| 5.01.2.2 | "Feedback must be provided on review questions or other content reinforcement tools. At a minimum, feedback must indicate that a response was 'correct' or 'incorrect'. The goal of providing feedback is to reinforce understanding, highlight knowledge gaps, and provide additional resources for learning comprehension." | 023 | `Question.feedback` (nullable text, `app/models/question.py`); `POST /courses/{slug}/lessons/{lessonSlug}/review/{question_id}` (`app/routers/review.py`, `app/schemas/review.py`) always returns the correct/incorrect verdict and returns the feedback text when present; `ReviewPanel.jsx` renders the verdict unconditionally and the text only when present | The verdict and optional text satisfy the stated minimum; nothing structures the feedback text itself toward "highlighting knowledge gaps" or "additional resources" beyond being free text an author writes - that phrasing is aspirational guidance for content quality, not a checkable structural requirement |
| 6.01.2 | "CPE program sponsors of self study programs must require participants to successfully complete a qualified assessment during or after the program with a cumulative minimum passing grade of at least 70 percent before issuing CPE credit for the course. ... At least 5 questions and scored responses per CPE credit must be included on the qualified assessment or 4 assessment questions and scored responses if the program is marketed for one-half CPE credits. For example, the qualified assessment for a 5-credit course must include at least 25 questions and scored responses. Alternatively, a 5 ½ credit course must include at least 29 questions and scored responses. Except in courses in which recall of information is the learning strategy, duplicate review and qualified assessment questions are not allowed. Forced choice responses such as 'True or false' or 'yes or no' questions are not permissible on the qualified assessment. ... [chart: 0.2→2, 0.4→3, 0.5→4, 0.6→4, 0.8→5, next full credit→5] ... does not use a test bank, whether or not feedback can be given depends on whether the participant passes the qualified assessment, then 1. on a failed assessment, the CPE program sponsor may not provide feedback to the test taker. 2. on assessments passed successfully, CPE program sponsors may choose to provide participants with feedback." | 023 | `Course.pass_ratio` (`numeric(3,2)`, CHECK `ck_courses_pass_ratio_floor` at `>= 0.70`, migration `19665ec864ab`) and `app/services/attempts.py::_pass_threshold` applied to an assessment-only question count; `app/constants/question_minimums.py` (`ASSESSMENT_PER_CREDIT`, `ASSESSMENT_MINIMUMS`) and `validate_for_publish`'s floor check, verified against both worked examples (`tests/test_question_minimums.py`); `MIN_CHOICES_ASSESSMENT = 3` in `validate_for_publish` for the forced-choice prohibition; the same-course exact-duplicate-prompt check between a review and an assessment question; `app/services/attempts.py`'s `AnswerResult` carries no correctness mid-attempt and `AttemptResultData.answers` is populated only on a pass - the no-test-bank feedback branch, by design, since abacadaba has no test bank | The above-one-credit decomposition (`whole * ASSESSMENT_PER_CREDIT + ASSESSMENT_MINIMUMS[remainder]`) is an interpretation of "additional qualified assessment questions ... are required based on the chart above," not a quoted rule - though it reproduces this paragraph's own worked examples exactly (5 credits → 25 questions, 5 ½ credits → 29 questions). The duplicate-prompt check catches **exact matches only** (whitespace/case normalized); a near-duplicate prompt is not caught and remains 021's reviewer-judgment territory, and the message returned to the author says so explicitly. The "except in courses in which recall of information is the learning strategy" exception is not modeled - there is no way to mark a course as using recall-as-strategy to exempt it from the duplicate check, so such a course cannot currently publish with an intentional duplicate. This feature implements only the no-test-bank feedback branch (traditional self study); the test-bank branch, nano learning's 100-percent/2-question path, and adaptive learning's fastest-completion-path minimum are all unimplemented, per current-feature.md's explicit no-test-bank scoping. **This row is superseded in two respects by feature 023a's row below**: it did not yet implement the 75%-of-learning-objectives coverage requirement at all, and the "AttemptResultData.answers populated only on a pass" it describes carried no feedback text on a pass either - only correctness - which under-delivers sub-ii b(2)'s actual permission |

### Feature 023a — Question feedback, objective coverage, and assessment integrity

Written after 023, whose row above this one it supersedes in two places (noted in that row's Gap column) rather than edits it, per this file's append-only rule.

| Locator | Requirement | Feature | Where in code | Gap |
| --- | --- | --- | --- | --- |
| 5.01.2.1 | "Review questions or other content reinforcement tools must be placed throughout the program in sufficient intervals to allow the participant the opportunity to evaluate the material that needs to be re-studied. At least three review questions or other content reinforcement tools with scored responses per CPE credit must be included. ... 'True or false' questions do not count toward the number of required review questions per CPE credit. There is no minimum passing rate required for review questions or other content reinforcement tools. ... [chart: 0.2→0, 0.4→1, 0.5→2, 0.6→2, 0.8→3, 1.0→3] ... After the first full credit and the minimum of three review questions or other content reinforcement tools with scored responses, additional review questions or other content reinforcement tools with scored responses are required based on the chart above." | 023a | No implementation change from 023's row above - this feature's own reconciliation step (`current-feature.md`, "A thing to check rather than assume") confirmed 023 already built the chart form, not a flat floor, so there was nothing to build here. Verified unchanged against the hazardous waste fixture (`tests/test_admin_content.py::test_hazardous_waste_fixture_publishes_at_both_credit_boundaries`), parametrized at 1.0 and 1.2 credit to exercise the above-one-credit addition rule, not just its base case | Same gaps as 023's row: the one-lesson placement collision and "other content reinforcement tools" being unimplemented, so only multiple choice can satisfy this floor |
| 5.01.2.2 | "Feedback must be provided on review questions or other content reinforcement tools. At a minimum, feedback must indicate that a response was 'correct' or 'incorrect'. The goal of providing feedback is to reinforce understanding, highlight knowledge gaps, and provide additional resources for learning comprehension." | 023a | 023's row already covered *serving* feedback once written; this feature covers *requiring* it be written. `validate_for_publish` (`app/services/admin_content.py`) now refuses to publish a review question whose `feedback` is blank or whitespace-only, closing the gap where a course could previously publish with an empty feedback textarea and silently fail this locator at runtime - proven by `tests/test_admin_content.py::test_publish_with_a_review_question_missing_feedback_is_refused` | Only presence is checked, not quality - "reinforce understanding, highlight knowledge gaps, and provide additional resources" is aspirational content guidance, not something a non-blank check can verify, same limitation 023's row already named |
| 6.01.2 | "A qualified assessment must measure a representative number of the learning objectives for the program. A representative number of the learning objectives is 75 percent or more of the learning objectives for the program. The representative number of the learning objectives can be less than 75 percent of the learning objectives for the program only if a randomized question generator is used, and the test bank used in the creation of the assessment includes at least 75 percent of the learning objectives for the program." and "on assessments passed successfully, CPE program sponsors may choose to provide participants with feedback. This feedback may comply with the type of feedback for review questions as described in Section 5 Paragraph 5.01.2.1 or take the form of identifying correct and incorrect answers." | 023a | `questions.objective_id` (migration `f86fae9d0a77`, FK to `learning_objectives.id`, `ondelete SET NULL` - deleting an objective untags its questions rather than deleting them); `app/services/objective_coverage.py::compute` (pure, read-only) and a new `validate_for_publish` rule refusing publish below 75%, naming the uncovered objectives by their own text; `QuestionEditor.jsx`'s "Learning objective" select (assessment questions only) and `ObjectivesPanel.jsx`'s live per-objective coverage count, both proven in a real browser session. On the feedback permission: `AnsweredQuestionResult`/`AnsweredQuestion` (`app/services/attempts.py`, `app/schemas/attempt.py`) now carry the question's `feedback` text, populated only when `get_result` builds the per-question breakdown at all - i.e. only on a pass, the same branch 023 built - and `AnswerBreakdown.jsx` renders it. Before this feature, a passed assessment's breakdown carried correctness but never the feedback text the Standard actually permits sponsors to add | Test banks do not exist in abacadaba, so the restrictive 6.01.2 sub-ii b) feedback path (no feedback on a fail, optional feedback on a pass) is the only one implemented - the sub-ii a) test-bank path, which permits feedback regardless of outcome, remains unbuilt and is named in a comment at `attempts.py::get_result`'s feedback-gating branch. The 75% coverage rule's own exception (below 75% permitted when a sufficiently large randomized test bank is used) is likewise not modeled, for the same reason. Objective tagging is an author's assertion that a question tests the objective it's tagged to - nothing validates that claim beyond the tag's presence, the same way nothing validates prompt/choice text quality elsewhere in this schema |

### Feature 024 — Completion documents and participant records

| Locator | Requirement | Feature | Where in code | Gap |
| --- | --- | --- | --- | --- |
| 9.01 | "CPE program sponsors must provide program participants with documentation (electronic or paper) of their participation (certificate of completion), which includes the following: 1) CPE program sponsor name 2) Participant's name 3) Course title 4) Date offered or completed 5) If applicable, location 6) Type of formal learning program 7) Amount of CPE credit recommended by field(s) of study 8) NASBA sponsor identification number 9) State registration number, if required by the state boards 10) NASBA time statement stating that CPE credits have been granted on a 50-minute hour 11) Any other statements required by boards of accountancy" | 024, 027 | `app/models/sponsor_profile.py` (sponsor name, NASBA registry ID, state registry ID(s)); nine `cert_*` columns on `app/models/attempt.py` written once at claim time by `claim_certificate` (`app/services/certificates.py`), never after; `render_pdf`'s labelled two-column block for the new fields, alongside the existing centred participant-name/course-title/date lines; `NASBA_TIME_STATEMENT` for item 10; `Verify.jsx` renders the same fields the PDF does, from the same `CertificateData` the PDF is built from. **Feature 027**: items 8 and 10 are no longer unconditional - `SponsorProfile.registry_status` (also snapshotted as `Attempt.cert_registry_status`) gates both. A certificate issued while the sponsor's `registry_status` is `"not_registered"` omits item 8 (no registry ID field at all, not blank) and item 10 (no NASBA time statement, since it would be false), and instead prints an unmistakable notice that the program is not offered by a registered sponsor and completion does not earn CPE credit - `render_pdf`'s `is_registered` branch and `NOT_REGISTERED_NOTICE_LINES` in `app/services/certificates.py`; `Verify.jsx` and `CourseDetail.jsx` (pre-enrollment) branch on the same field | Item 5 ("if applicable, location") is not modeled - self study is asynchronous by the Standards' own definition and has no location to record. Item 11 ("any other statements required by boards of accountancy") is not modeled - no per-state statement requirement exists anywhere in this application. Item 10's statement is fixed, sponsor-wide text, not configurable per state or per course. **Feature 027**: nothing verifies a sponsor's self-reported `registry_status` against the actual National Registry - an admin can set it to `"registered"` and type any registry ID. What changed is that a false claim is now a deliberate act (an admin has to select "Registered" and is told what that means) rather than a side effect of a publish gate that left no honest option; that is the limit of what this feature does. |
| 9.01.1 | "CPE program sponsors must provide participants with documentation (electronic or paper) to support their claims of CPE credit. Acceptable evidence of completion includes the following: ... 2) For self study and nano learning programs, a certificate supplied by the CPE program sponsor after satisfactory completion of a qualified assessment" | 007, 024 | `app/services/attempts.py`'s pass/completion gate (feature 003/007) blocks `claim_certificate` (feature 007) until an attempt is `completed_at is not None and passed`; feature 024 backs the resulting certificate with the sponsor identity and frozen course facts this evidence needs to actually be checkable on audit | Nano learning is not implemented - `cert_delivery_method` is provisioned for it (current-feature.md's own reasoning) but abacadaba only ever produces "QAS Self Study" certificates today. Group/blended-program and published-article evidence (items 1, 3, 4) are out of scope entirely; abacadaba only offers self study. |
| 9.02 | "CPE program sponsors must retain adequate documentation (electronic or paper) for a minimum of five years to support their compliance with these Standards and the reports that may be required of participants." | 024 | `RETENTION_YEARS = 5` in `app/constants/retention.py` | Recorded, not enforced - nothing purges data at five years or blocks deletion before then; current-feature.md scoped this to recording the period, not building a retention policy. 9.02.1's group-program-specific documentation elements are out of scope, since abacadaba only offers self study, not group programs. |
| 6.01 | "Although it is the CPA's and other professional's responsibility to report the appropriate number of credits earned, CPE program sponsors must maintain a process to monitor individual attendance at group and blended learning programs or verify individual successful program completion for self study and blended learning programs to assign the correct number of CPE credits. Self-certification of attendance/completion alone is not sufficient." | 016, 024 | `require_user` on `POST /courses/{slug}/attempts` (feature 016, `app/routers/courses.py`) means every attempt created since 2026-08-11 is tied to an authenticated account rather than a self-reported name; `app/services/certificates.py::claim_certificate` still branches to a typed-in `recipient_name` when `attempt.user is None` | That branch is still reachable for any attempt created before feature 016 (2026-08-11) that is unclaimed or gets claimed today - a typed name on those is exactly the self-certification this locator says is not sufficient. The gap is bounded, not open-ended: no new anonymous attempt can be created going forward, so it can only shrink. This feature deliberately did not remove that branch (current-feature.md: an auth/enrolment decision, out of scope here) and does not consider 6.01 satisfied for any certificate whose name came from it. |

### Feature 025 — Evaluations

| Locator | Requirement | Feature | Where in code | Gap |
| --- | --- | --- | --- | --- |
| 4.04 | "CPE program sponsors must employ an effective means for evaluating learning activity quality with respect to content and presentation, as well as provide a mechanism for participants to assess whether learning objectives were met." | 025 | `app/models/evaluation.py` (`evaluations` table, one row per attempt via a unique FK on `attempt_id`); `EvaluationForm.jsx` renders on both branches of `Result.jsx` (passed and failed) so every participant is offered it once their attempt completes; `POST`/`GET /attempts/{id}/evaluation` (`app/routers/evaluations.py`); `GET /admin/courses/{id}/evaluations` (`app/routers/admin_analytics.py`) and `Evaluations.jsx` give the sponsor the means to see program quality signal per course | |
| 4.04.1 | "Required elements of evaluation. The objectives of evaluation are to assess participant and instructor satisfaction with specific programs and to increase subsequent program effectiveness. Evaluations, whether written or electronic, must be solicited from participants and instructors for the overall program. Sponsors may elect to solicit evaluations for each session within an overall program. Evaluations determine, among other things, whether 1) stated learning objectives were met. 2) stated prerequisite requirements were appropriate and sufficient. 3) program materials, including the qualified assessment, if any, were relevant and contributed to the achievement of the learning objectives. 4) time allotted to the learning activity was appropriate. 5) instructors were effective, where applicable." | 025 | `app/constants/evaluation_dimensions.py`'s five ordered `EvaluationDimension` records (key, question text, `applies_to_self_study`), served unauthenticated via `GET /meta/evaluation-dimensions` so the frontend never hand-codes the list; five nullable, CHECK-constrained (1-5) columns on `evaluations` map 1:1 to items 1-4 (`objectives_met`, `prerequisites_appropriate`, `materials_relevant`, `time_allotted_appropriate`) plus a free-text `comments` field beyond the enumerated floor; solicited from participants only, for the overall program (not per-session) | Item 5, instructor effectiveness, is deliberately not solicited. abacadaba's self study programs have no instructor a participant interacts with - Section 1's definition of "instructor" is broad enough that video narration could arguably qualify, but rating an instructor never met would produce noise, not quality data, so `dimensions_for_self_study()` filters `instructor_effective` out of the served list and the participant-facing form never renders or collects it. The column and constant entry are kept, not deleted (`applies_to_self_study = False`), so superCPE's group programs can turn it on later. Separately, this locator's "must be solicited from ... instructors" clause is not addressed at all - self study has no instructor to solicit an evaluation *from*, either, and no such mechanism exists here. |
| 4.04.2 | "Evaluation results. CPE program sponsors must periodically review evaluation results to assess program effectiveness and should inform developers and instructors of evaluation results." | 025 | `app/services/evaluations.py::course_summary`/`course_comments` and `GET /admin/courses/{id}/evaluations` make every course's response count, response rate, per-dimension means, and comments available to an admin on demand | This feature builds availability, not the review itself. Nothing schedules, reminds, or records that a periodic review took place, and nothing connects a course's evaluation results to its `developer_id`/`reviewer_id` (feature 021) to inform them - nothing is architecturally "informed" of anything today. Treated as a human obligation the software makes possible rather than one it performs, the same posture feature 024 took for 9.02's retention period versus enforcing it - a deliberate scoping decision, not an oversight, but this row records it as open work rather than satisfied. |

### Feature 026 — Policies, disclosures, and content currency

Closes 8.01.1's original gap (row updated above, in feature 020's section) and
updates 4.02's gap to point at the dashboard it forward-referenced (row
updated above, in feature 021's section) rather than duplicating either row.
The two rows below are new: 4.01's row in feature 021's section forward-referenced
this feature by name for review-cycle enforcement; this row is what it built.
9.02.2 had no row at all before this feature.

| Locator | Requirement | Feature | Where in code | Gap |
| --- | --- | --- | --- | --- |
| 4.01 | "CPE program sponsors must employ activities, materials, and delivery systems that are current, accurate, and effectively designed. Course documentation must contain the most recent publication, revision, or review date. Courses in subjects that undergo frequent changes such as updates to codes, laws, rulings, decisions, interpretations, etc. must be reviewed and revised, as necessary, by a subject matter expert as soon as possible but at least once a year to verify the currency of the content. Other courses must be reviewed and revised, as necessary, at least every two years." | 026 | `app/constants/currency.py` (`REVIEW_WINDOW_DAYS` mapping `Course.review_cycle`'s `'annual'`/`'biennial'` to 365/730 days, `DUE_SOON_WINDOW_DAYS = 60`); `app/services/currency.py::overdue_review`/`due_soon` compare `reviewed_at + window` against the clock, the comparison feature 021's row said did not yet exist; `GET /admin/currency` (`app/routers/admin_currency.py`, behind `require_admin`) and `AdminCurrency.jsx`'s "Overdue for review"/"Due for review soon" sections, sorted worst-first, reusing `Stats.jsx`'s table treatment rather than a second one; linked from the admin nav, not from inside any one course | Reporting only, the same posture as 4.02's row above: nothing here blocks a course from staying published, or from being freshly published, once its review window has lapsed. `validate_for_publish` (021) only checks `reviewed_at >= content_updated_at` - that a review postdates the last edit - never that `reviewed_at` is still inside its `review_cycle`'s window, so a course that is reviewed once and never edited again can sit published and overdue indefinitely and nothing refuses to (re-)publish it on that basis alone. No reminders or notifications exist - current-feature.md ruled that out explicitly ("Emailing anyone about anything overdue"). `overdue_review`/`due_soon` are not filtered to `is_published` courses, so a draft reviewed long ago and never published also appears; each row's own Published/Draft column is how a sponsor tells the two apart, since a draft has no participants but still has a stale review date. |
| 9.02.2 | "Course documentation must include an expiration date (the time by which the participant must complete the qualified assessment). For individual courses, the expiration date is no longer than one year from the date of purchase or enrollment. For a series of courses to achieve an integrated learning plan, the expiration date may be longer." | 026 | `Course.expires_on` (`app/models/course.py`, migration `c101e414d784`); `validate_for_publish` refuses to publish without it; `app/services/attempts.py::start_attempt` raises `CourseExpiredError` before it even checks whether anyone is signed in - 9.02.2 is a property of the course, not the participant, so it is checked first, ahead of feature 019's authenticate-then-gate-then-policy order - and the router (`app/routers/courses.py`) turns that into a 403 with the expiration date named, never a 404; `app/services/courses.py::list_published` excludes an expired course from the public catalog while leaving it reachable at its own URL and not force-unpublished (`GET /courses/{slug}` still 200s), the same non-destructive posture feature 021 took for a stale review; `CourseDetail.jsx` discloses it beside credit and last-reviewed date and replaces the assessment button with the expiration reason once past it; `CourseDetailsForm.jsx` defaults the field to `reviewed_at` plus `EXPIRATION_WINDOW_DAYS` (365, mirrored from `app/constants/currency.py`) the first time a review is recorded, editable afterward; `AdminCurrency.jsx`'s "Expired or expiring" section (`app/services/currency.py::expired_or_expiring`) surfaces every expired-or-soon-to-expire course sponsor-wide | abacadaba has no per-participant purchase or enrollment event - a self study course is browsed and started whenever a participant likes - so "no longer than one year from the date of purchase or enrollment" is measured from the course's own review date instead, the nearest thing this system has to "when this content was current as of." That is a real, disclosed substitution, not a hidden one: a participant who starts a course shortly before `expires_on` gets meaningfully less than a year to finish it, where the Standard's own per-enrollment framing would have given them closer to the full year. The "series of courses to achieve an integrated learning plan" exception (a longer expiration for a bundled program) is not modeled - abacadaba has no course-series or program-bundle concept, only individual courses. This row is scoped to item 3 (the expiration date) only; 9.02.2's other documentation elements are covered elsewhere or fall under 9.02's five-year retention window (feature 024's `RETENTION_YEARS`, now also disclosed publicly via the `records-retention` policy page above). |

## Open Gaps

Every locator above whose Gap column still names outstanding work, as of this
feature, in one list rather than twenty-seven rows. A locator not listed here
either has an empty Gap column or its Gap column now only records a
confirmed not-applicable finding (8.01.1's schedule-of-events clause).

- **3.01.1** - no mandatory-internal-course concept exists (the 8.01 exception is unmodeled)
- **3.02.1** - "precise language" for prerequisites is not validated, only presence
- **4.01** (021's row) / **4.01** (026's row) - review-cycle currency is now reported (026) but not enforced at (re-)publish time, and nothing reminds a sponsor
- **4.01.1** - a developer's self-reported credentials are not verified against any state board
- **4.02** - a published-then-edited course keeps serving unreviewed content; 026 reports this sponsor-wide but does not prevent it
- **4.02.1** - "qualified in the subject matter" and the rare-impractical-review case are editorial judgment, not structured fields
- **7.01** - only the one-fifth increment path is implemented, uniformly, not per-jurisdiction
- **7.02.6** - word count is author-typed, not checked against actual segment text
- **7.02.7** - nothing verifies a lesson's `duration_seconds` came from a measured render rather than a typed estimate
- **5.01.2** / **6.01.2** (023/023a) - simulations and other non-multiple-choice content reinforcement/assessment tools are not modeled; a one-lesson course above 0.2 credit has no "throughout the program" seam to place review questions in
- **5.01.2.2** - feedback presence is checked, not the quality of what it says
- **6.01.2** (023a) - the test-bank feedback arm and the below-75%-coverage-with-a-test-bank exception are unbuilt; objective tagging is an unverified author assertion
- **9.01** - item 11 (state-board-specific certificate statements) is not modeled
- **9.01.1** - nano learning is unimplemented; group/blended-program evidence is out of scope (self study only)
- **9.02** - the five-year retention period is recorded and now published (026), not enforced - nothing purges or blocks deletion at the boundary
- **9.02.2** - measured from review date, not per-participant enrollment date; the integrated-learning-plan series exception is unmodeled
- **6.01** - self-certified pre-016 anonymous certificates remain a bounded, shrinking gap
- **4.04.1** - instructor evaluation is deliberately not solicited (self study has no instructor)
- **4.04.2** - nothing schedules or records that a periodic evaluation review took place, or connects results to the developer/reviewer
